BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[Song] ADD [pathImage] NVARCHAR(1000) NOT NULL CONSTRAINT [Song_pathImage_df] DEFAULT 'https://via.placeholder.com/150';

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
